Change the success label to green tick mark.
Change the success description to nice UI friendly description.
Change the arrays symbol icon in testcasegenerator page
Change the header in testcasegenerator page
Put placeholders in all sub category forms
Remove the errors from span tags, as soon as user presses on any field/button
Add whiteSpaceCharacter input field to strings category
Remove placeholders in arrays and matrix categories (Rakesh)
Remove the alert function call in JS when arrays and matrix return successful json response (Rakesh)
Change the back-to-top link in the home page of our application to something which looks good
Put a footer in TestCaseGenerator main page (since the page at the bottom seems empty)
Put some background color on all the sub-category forms (Plain white is kind of dull)
